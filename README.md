# BdaySender

Web MVP implementation lives in `bdaysender-web`.

Run locally:

```bash
cd bdaysender-web
npm install
npm run dev
```

Deployment target is Vercel, with cron + Gmail SMTP configured from env vars.
